For a current, actively maintained reference implementation, look in `ARankB/`
— it demonstrates trap questions, server-driven refresh-resume (query_id
counted on answer), and CSV response formatting. See DEV_DOCUMENTATION.md at
the repository root for the full walkthrough.

For historical examples, look in the directory `PoolBasedTripletMDS/`. This is the best
documented app of the original framework (this directory structure was developed around it).

Scott Sievert,
2016-1-14
during the creation of `next/apps/`
